apiVersion: v1
kind: Service
metadata:
  name: {{ include "asl-bissieux.fullname" . }}
  labels:
    {{- include "asl-bissieux.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "asl-bissieux.selectorLabels" . | nindent 4 }}
